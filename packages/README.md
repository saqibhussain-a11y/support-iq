# packages/

Shared code used by more than one app in `apps/`.

Nothing lives here yet. Likely future candidates: shared TypeScript types for the
ticket/classification/response API contracts (Module 5+), shared eval fixtures (Module 8).
Don't add a package here speculatively — wait until frontend and backend actually need to
share something.
